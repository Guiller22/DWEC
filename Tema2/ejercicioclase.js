var modulos1 = ['prog'  , 'fol' , 'bases de datos' , 'lenguaje de marcas', 'entornos de desarrollo' , 'sistemas informaticos' , 'entorno cliente' , 'diseño' , 'entorno servidor' , 'despliegue', 'empresa'];
var intentos=0;
function modulos(nombre) {
    this.nombre=nombre;
    this.nota=0;
}
function alumno(nombre, ) {
    this.nombre = nombre;
    this.curso="";
    this.anyo="2019";
    this.nota1=0;
    this.nota2=0;


    this.examinar = function (){
        if(curso=1 )
        while (this.nota = 0 && intentos<=2) {
            var nota = Math.floor(Math.random());
            this.anyo++;
            console.log(nota);
            
        }
    }
}





var clase = [];

while (clase.every(y => y.vueltas < vueltasTotales)) {
    if(clase.every(x => )) {
        console.log("TODOS SIN GASOLINA");
        break;
    }
}